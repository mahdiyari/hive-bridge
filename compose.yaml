services:
  hive-bridge:
    image: mahdiyari/hive-bridge:0.0.1
    container_name: hive-bridge
    ports:
      - "${DOCKER_PORT}:3018"
    env_file:
      - .env
    environment:
      - ENV_VAR=production
    restart: unless-stopped
