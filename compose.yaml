services:
  hive-bridge:
    image: mahdiyari/hive-bridge:0.0.1
    container_name: hive-bridge
    ports:
      - ${P2P_PORT}
    env_file:
      - .env
    environment:
      - ENV_VAR=production
    restart: unless-stopped
